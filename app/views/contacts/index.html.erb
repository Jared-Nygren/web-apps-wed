<h1>Contacts</h1>

<% contacts = Contact.all%>

<p> There are <%=Contact.count%> contacts <p>


<ul>
<% for contact in contacts %>
<% company = Company.find_by({"id" => contact["company_id"]})%>
   <li><%= contact["first_name"] %> at <%= company["name"]%> </li>
  <% end %>
<ul>